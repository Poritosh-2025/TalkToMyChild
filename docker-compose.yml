services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: django_api
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234betopia
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    volumes:
      - .:/app
      - media_data:/app/media
    ports:
      - "12004:12004"
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  celery_worker:   # MUST be inside services
    build: .
    command: ./entrypoint.sh worker
    env_file:
      - .env
    depends_on:
      web:
        condition: service_started
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  celery_beat:     # MUST be inside services
    build: .
    command: ./entrypoint.sh beat
    env_file:
      - .env
    depends_on:    
      web:
        condition: service_started
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  postgres_data:
  media_data: